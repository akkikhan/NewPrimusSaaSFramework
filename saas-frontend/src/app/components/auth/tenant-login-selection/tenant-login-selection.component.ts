import { Component } from '@angular/core';

@Component({
  selector: 'app-tenant-login-selection',
  standalone: true,
  template: `<p>Tenant Login Selection works!</p>`
})
export class TenantLoginSelectionComponent {} 